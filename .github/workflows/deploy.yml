name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build web image
        run: |
          docker build \
            -f Dockerfile.web \
            --build-arg VITE_PUBLIC_VIEWER_MODE=false \
            --build-arg VITE_GOOGLE_FORM_URL='https://docs.google.com/forms/d/e/1FAIpQLSdRR27bQDytT0xJNa2z6YSqksiclxtgoKxbaY-uANlAZGgeag/viewform?usp=dialog' \
            --build-arg VITE_GOOGLE_SHEET_QUEUE_URL='https://docs.google.com/spreadsheets/d/13CZxAD8lctUtJEcVHH-qUHKNIJY0ENxcxPP-DwNgelE/edit?usp=sharing' \
            -t saralray/printfarm-web:latest .

      - name: Build poller image
        run: |
          docker build \
            -f Dockerfile.poller \
            -t saralray/printfarm-poller:latest .

      - name: Build nginx image
        run: |
          docker build \
            -f Dockerfile.nginx \
            -t saralray/printfarm-nginx:latest .

      - name: Push images
        run: |
          docker push saralray/printfarm-web:latest
          docker push saralray/printfarm-poller:latest
          docker push saralray/printfarm-nginx:latest

      - name: Setup kubeconfig
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBE_CONFIG }}" > ~/.kube/config

      - name: Apply manifests
        run: |
          kubectl apply -f k8s/

      - name: Restart deployments
        run: |
          kubectl rollout restart deployment/web deployment/poller deployment/nginx -n printfarm
          kubectl rollout status deployment/web -n printfarm --timeout=120s
          kubectl rollout status deployment/nginx -n printfarm --timeout=120s